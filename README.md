"# nitkkrbot_2.0"
"# CollegeGPT"

## Setup

1. Copy `.env.example` to `.env`.
2. Set `GEMINI_API_KEY` and `PINECONE_API_KEY` in `.env`.
3. Do not commit `.env` to GitHub.

The repository already ignores `.env` and `.env.*` in `.gitignore`.
